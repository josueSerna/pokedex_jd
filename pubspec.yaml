name: pokemon_api_v1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  http: ^1.6.0
  atomic_ds_system_jd:
    git:
      url: https://github.com/josueSerna/atomic_ds_system_jd

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
